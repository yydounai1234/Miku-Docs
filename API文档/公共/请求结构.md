# 请求结构

## 服务地址
- 列举空间：mls.cn-east-1.qiniumiku.com
- 其他: \<bucket\>.mls.cn-east-1.qiniumiku.com

## 通信协议
  支持通过 HTTP 或 HTTPS 进行通信，为了更高的安全性，建议使用 HTTPS 发送API请求。

## 字符编码
  请求及返回结果都使用 UTF-8 字符集进行编码。