# 查询直播流量

## 接口名称
查询直播流量

## 接口描述
查询指定时间范围内的直播流量统计信息

## 请求方法
GET

## 请求路径
```
GET /?statistic=traffic&live
```

## 请求参数
| 参数名称 | 类型 | 是否必填 | 说明 |
|---------|------|---------|------|
| startTime | string | 是 | 开始时间，ISO8601格式 |
| endTime | string | 是 | 结束时间，ISO8601格式 |
| bucket | string | 否 | 空间名称，不指定则查询所有空间 |
| domain | string | 否 | 域名，不指定则查询所有域名 |
| direction | string | 否 | 流量方向，可选值：up（上行）, down（下行） |

## 请求示例
```http
GET /?statistic=traffic&live&startTime=2023-01-01T00:00:00Z&endTime=2023-01-01T23:59:59Z&bucket=test-bucket&direction=down HTTP/1.1
Host: <service-code>.<region-code>.qiniumiku.com
Authorization: <QiniuToken>
Content-Type: application/json
```

## 响应参数
| 参数名称 | 类型 | 说明 |
|---------|------|------|
| requestId | string | 请求ID |
| code | integer | 响应码 |
| message | string | 响应消息 |
| data | object | 流量统计信息 |
| data.totalTraffic | integer | 总流量（字节） |
| data.trafficList | array | 流量列表 |
| data.trafficList[].timestamp | string | 时间戳 |
| data.trafficList[].traffic | integer | 流量（字节） |
| data.trafficList[].bucket | string | 空间名称 |
| data.trafficList[].domain | string | 域名 |
| data.trafficList[].direction | string | 流量方向 |

## 响应示例
```json
{
  "requestId": "558f0655-f373-48e6-93fb-7c91020462e0",
  "code": 200,
  "message": "success",
  "data": {
    "totalTraffic": 10737418240,
    "trafficList": [
      {
        "timestamp": "2023-01-01T00:00:00Z",
        "traffic": 1073741824,
        "bucket": "test-bucket",
        "domain": "play.example.com",
        "direction": "down"
      },
      {
        "timestamp": "2023-01-01T01:00:00Z",
        "traffic": 2147483648,
        "bucket": "test-bucket",
        "domain": "play.example.com",
        "direction": "down"
      }
    ]
  }
}
```

## 错误码
| 错误码 | 说明 |
|-------|------|
| 400 | 请求参数错误 |
| 401 | 鉴权失败 |
| 500 | 服务端内部错误 |

## 鉴权方式
支持 QiniuToken、IAM 鉴权和 Apikey 鉴权，请参考[请求鉴权](../../公共API规范/请求鉴权.md)文档。