# 推流质量分析

## 接口名称
推流质量分析

## 接口描述
分析指定时间范围内直播流的推流质量

## 请求方法
GET

## 请求路径
```
GET /?statistic=quality&direction=publish&startTime=<startTime>&endTime=<endTime>
```

## 请求参数
| 参数名称 | 类型 | 是否必填 | 说明 |
|---------|------|---------|------|
| startTime | string | 是 | 开始时间，ISO8601格式 |
| endTime | string | 是 | 结束时间，ISO8601格式 |
| bucket | string | 否 | 空间名称，不指定则查询所有空间 |
| streamId | string | 否 | 流ID，不指定则查询所有流 |
| domain | string | 否 | 域名，不指定则查询所有域名 |

## 请求示例
```http
GET /?statistic=quality&direction=publish&startTime=2023-01-01T00:00:00Z&endTime=2023-01-01T23:59:59Z&bucket=test-bucket HTTP/1.1
Host: <service-code>.<region-code>.qiniumiku.com
Authorization: <QiniuToken>
Content-Type: application/json
```

## 响应参数
| 参数名称 | 类型 | 说明 |
|---------|------|------|
| requestId | string | 请求ID |
| code | integer | 响应码 |
| message | string | 响应消息 |
| data | object | 质量分析结果 |
| data.totalStreams | integer | 总流数 |
| data.qualityStats | object | 质量统计 |
| data.qualityStats.excellent | integer | 优秀质量流数 |
| data.qualityStats.good | integer | 良好质量流数 |
| data.qualityStats.fair | integer | 一般质量流数 |
| data.qualityStats.poor | integer | 较差质量流数 |
| data.qualityStats.bad | integer | 很差质量流数 |
| data.qualityDetails | array | 质量详情 |
| data.qualityDetails[].streamId | string | 流ID |
| data.qualityDetails[].bucket | string | 空间名称 |
| data.qualityDetails[].qualityScore | number | 质量评分（0-100） |
| data.qualityDetails[].bitrate | integer | 平均码率（bps） |
| data.qualityDetails[].framerate | number | 平均帧率 |
| data.qualityDetails[].resolution | string | 分辨率 |
| data.qualityDetails[].dropFrames | integer | 丢帧数 |
| data.qualityDetails[].reconnectCount | integer | 重连次数 |

## 响应示例
```json
{
  "requestId": "558f0655-f373-48e6-93fb-7c91020462e0",
  "code": 200,
  "message": "success",
  "data": {
    "totalStreams": 150,
    "qualityStats": {
      "excellent": 120,
      "good": 25,
      "fair": 3,
      "poor": 2,
      "bad": 0
    },
    "qualityDetails": [
      {
        "streamId": "test-stream-001",
        "bucket": "test-bucket",
        "qualityScore": 95.5,
        "bitrate": 2000000,
        "framerate": 30.0,
        "resolution": "1920x1080",
        "dropFrames": 0,
        "reconnectCount": 0
      },
      {
        "streamId": "test-stream-002",
        "bucket": "test-bucket",
        "qualityScore": 82.3,
        "bitrate": 1500000,
        "framerate": 25.0,
        "resolution": "1280x720",
        "dropFrames": 5,
        "reconnectCount": 1
      }
    ]
  }
}
```

## 错误码
| 错误码 | 说明 |
|-------|------|
| 400 | 请求参数错误 |
| 401 | 鉴权失败 |
| 500 | 服务端内部错误 |

## 鉴权方式
支持 QiniuToken、IAM 鉴权和 Apikey 鉴权，请参考[请求鉴权](../../公共API规范/请求鉴权.md)文档。